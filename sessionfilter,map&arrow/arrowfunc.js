// regular function 
function sum(a,b) { 
    const sum = a + b; 
    return sum; 
}

// arrow functions
// things get different when we do callbacks 
const sum = (a,b) =>{ 
    const ans = a + b; 
    return ans; 
}